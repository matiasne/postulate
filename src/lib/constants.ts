/** Límite razonable de referencias que un mismo usuario puede dejar a un candidato. */
export const MAX_REFERENCES_PER_CANDIDATE = 3;

/** Centro aproximado de Río Tercero, Córdoba (fallback del mapa). */
export const RIO_TERCERO_CENTER: [number, number] = [-32.1747, -64.112];

export const LEMA = "mérito y valor";
